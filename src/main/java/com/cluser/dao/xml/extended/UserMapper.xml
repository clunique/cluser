<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nationsky.edp.usercenter.dao.mapper.extended.UserMapper">
	<resultMap id="BaseResultMap" type="com.nationsky.edp.usercenter.dao.model.generated.TUser"/>
	<select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
		select * from t_user where user_name=#{userName}
	</select>

	<select id="selectValidUserIds" resultType="long">
	select distinct id from t_user WHERE id in
		<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
			#{item}
		</foreach>
	</select>
</mapper>
